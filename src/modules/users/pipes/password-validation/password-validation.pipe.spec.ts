import { PasswordValidationPipe } from './password-validation.pipe';

describe('PasswordValidationPipe', () => {
  it('should be defined', () => {
    expect(new PasswordValidationPipe()).toBeDefined();
  });
});
